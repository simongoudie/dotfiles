"if ! has("gui_running") 
"   set t_Co=256 
"endif 
"set background=dark
"colorscheme solarized 

if has ("gui_running")
  set guifont=Inconsolata:h11
  set background=dark
  colorscheme solarized
  set cursorline
endif
